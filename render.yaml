services:
  - type: web
    name: fastapi-backend
    env: docker
    plan: free
    region: oregon
    dockerfilePath: backend/Dockerfile
    healthCheckPath: /
    autoDeploy: true
    envVars:
      - key: PORT
        value: 8000
